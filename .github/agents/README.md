# Agents README

This project uses several specialized agents to help with development tasks. New policy:

- Any new or changed lines introduced by agents must have >=80% coverage on the diff. Enforcement: see `.github/workflows/enforce-new-code-coverage.yml`.
